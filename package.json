{
  "dependencies": {
    "axios": "^1.0.0",
    "chai": "^4.5.0",
    "node-fetch": "^2.7.0"
  },
  "scripts": {
    "test:integration": "npx cucumber-js local-tests/features/*.feature --import local-tests/step_definitions/*.js --format json:reports/cucumber-report.json"
  },
  "devDependencies": {
    "@cucumber/cucumber": "^9.0.0"
  }
}
